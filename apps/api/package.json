{
  "name": "@roam/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm exec turbo run build --filter=@roam/db",
    "build": "tsc",
    "dev": "tsx watch --env-file=.env src/index.ts",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "@roam/db": "workspace:*",
    "@roam/types": "workspace:*",
    "hono": "^4.6.0",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
