# Root — copy to .env and fill in values
# All consumers use these key names; keep consistent.

# Supabase (required for API/db; optional for web/mobile anon-only)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Local dev
NODE_ENV=development
